# Autonomous workflow

The intended workflow is: detect commitment → retrieve details → retrieve location → resolve destination → estimate route → retrieve preparation profile → calculate thresholds → decide → act → verify movement → re-evaluate → learn.

See [AGENT_BEHAVIOR.md](AGENT_BEHAVIOR.md) and [TESTING_STRATEGY.md](TESTING_STRATEGY.md).

