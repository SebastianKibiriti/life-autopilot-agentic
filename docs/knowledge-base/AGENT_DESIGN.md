# Agent design

The agent is a single bounded Collaborative Partner with explicit tools, states, decisions, and persistent user memory. Deterministic planning happens before contextual reasoning; actions are observable and persist structured events. Gemini supplies contextual synthesis while bounded code controls timing and side effects.

See [AGENT_BEHAVIOR.md](AGENT_BEHAVIOR.md) and [ARCHITECTURE.md](ARCHITECTURE.md).
